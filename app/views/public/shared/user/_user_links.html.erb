<!-- リンク集、使い方:  render '~/user_links', 変数名: == true (, as: :button) 
     ()内はボタンにしたいときだけ記載、テキストの場合は()もいらない -->

<% is_button = (local_assigns[:as] == :button) %>
<% btn_class = 'btn btn-base' %>

<% if defined?(home) && home %>
  <%= link_to 'ホーム', root_path, class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(profile_page) && profile_page %>
  <%= link_to '公開マイページ', user_path(current_user), class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(show_edit) && show_edit %>
  <%= link_to 'プロフィールを編集する', edit_user_path, class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(mypage) && mypage %>
  <%= link_to 'マイページ', mypage_user_path, class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(settings) && settings %>
  <%= link_to '登録情報を編集する', settings_user_path, class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(withdraw) && withdraw %>
  <%= link_to '退会する', check_user_path, class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(profile) && profile %>
  <%= link_to '自身のプロフィールを見る', user_path(current_user), class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(my_posts_index) && my_posts_index && defined?(@user) %>
  <%= link_to '自分の投稿一覧を見る', user_posts_path(current_user), class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(all_posts_index) && all_posts_index %>
  <%= link_to 'すべての投稿一覧を見る', user_all_posts_path, class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(new_post) && new_post && defined?(@user) %>
  <%= link_to '新規投稿', new_user_post_path(current_user), class: (is_button ? btn_class : nil) %>
<% end %>

<% if defined?(back) && back %>
  <%= link_to '戻る', request.referer || root_path, class: (is_button ? btn_class : nil) %>
<% end %>