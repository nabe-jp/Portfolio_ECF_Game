<%= form_with model: @membership, url:  @form_url, local: true do |f| %>
  <div class="form-group">
    <%= f.label :note, "管理者メモ" %>
    <%= f.text_area :note, rows: 5, class: "form-control", 
      placeholder: group_member_note_placeholder %>
  </div>
  <%= f.submit "メモを更新", class: "btn btn-sm btn-outline-primary" %>
<% end %>