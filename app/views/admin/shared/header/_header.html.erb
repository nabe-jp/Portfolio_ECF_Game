<header class="container-fluid">
  <div class="row">
    <div class="col-2 mt-2 d-flex justify-content-start offset-1 align-items-center">
      <%= image_tag("logo.jpg", size: "120x80") %>
    </div>
    <div class="col-6 mt-1">
      <%#= render 'admin/shared/header/global_search' %>                   <!-- 検索フォーム -->
    </div>
    <div class="col-3">
        <div class="d-flex justify-content-end mt-1"> 
        <ul class="nav">
          <% if admin_signed_in? %>                                 <!-- 管理者がサインインした場合 -->
            <%= render 'admin/shared/header/admin_nav' %>
          <% else %>                                                <!-- 誰もサインインしていない場合 -->
            <%= render 'admin/shared/header/no_user_nav' %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</header>