<!DOCTYPE html>
<html>
  <head>
    <title>PortfolioEcfGame</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- レスポンシブデザイン用、
      ページとデバイスの画面幅を合わせ、スケールを1に設定、ユーザーがズームインできなくする -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <!-- WebpackerによるJavaScriptとCSSをバンドルして読み込む -->
    <script src="https://kit.fontawesome.com/ecc368403e.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <style>
    /* テスト用 */
    

:root {
  --mobile-menu-top-margin: 0.25rem;
}

.mobile-menu-wrapper {
  overflow: visible;
}

#mobileMenu {
  transform: scaleY(0);
  transform-origin: top;
  opacity: 0;
  pointer-events: none;
  transition: transform 0.5s ease, opacity 0.5s ease;

  position: absolute;
  right: 0;
  top: var(--header-height);
  background-color: rgba(255, 255, 255, 0.9);
  z-index: 1100;
  margin-top: 0.5rem;
  padding: var(--mobile-menu-top-margin);
  border-radius: 4px;
  max-width: 40vw;
  overflow: hidden;
}

#mobileMenu.show {
  transform: scaleY(1);
  opacity: 1;
  pointer-events: auto;
  max-height: calc(100vh - var(--header-height) + var(--mobile-menu-top-margin));
  max-width: 40vw;
}

  </style>
  </head>

  <!-- bodyを動的にし、bodyが短くなり見栄えが悪くならないように高さを取る -->
  <body class="d-flex flex-column <%= controller.controller_name %> <%= controller.action_name %> vh-100">
  
    <!-- ヘッダー -->
    <%= render 'public/shared/header/header' %>
    <div class="container">
      <div class="row">

        <!-- 左サイドバー -->
        <div class="sidebar sidebar-left">
          <%= render 'public/shared/sidebar/left_sidebar' %>
        </div>

        <!-- メインコンテンツ -->
        <main class="main-content">

          <!-- メッセージ -->
          <span class="d-flex justify-content-center"><%= render 'shared/message/messages' %></span>
          <%= yield %>
        </main>

        <!-- 右サイドバー -->
        <div class="sidebar sidebar-right">
          <%= render 'public/shared/sidebar/right_sidebar' %>
        </div>
      
      </div>
    </div>
  </body>
</html>