<!DOCTYPE html>
<html>
  <head>
    <title>PortfolioEcfGame</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- レスポンシブデザイン用、
      ページとデバイスの画面幅を合わせ、スケールを1に設定、ユーザーがズームインできなくする -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <!-- WebpackerによるJavaScriptとCSSをバンドルして読み込む -->
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <!-- bodyを動的にし、bodyが短くなり見栄えが悪くならないように高さを取る -->
  <body class="d-flex flex-column <%= controller.controller_name %> <%= controller.action_name %> vh-100">
  
    <!-- ヘッダー -->
    <%= render 'shared/header/header' %>
    <div class="container">
      <div class="row">

        <!-- 左サイドバー -->
        <div class="sidebar sidebar-left">
          <h5>ナビゲーション</h5>
          <ul class="nav flex-column">
            <li class="nav-item"><%= link_to 'ホーム', root_path, class: 'nav-link' %></li>
            <li class="nav-item"><%= link_to 'マイページ', user_path(current_user), class: 'nav-link' if user_signed_in? %></li>
          </ul>
        </div>

        <!-- メインコンテンツ -->
        <main class="col-9 main-content">

          <!-- メッセージ -->
          <span class="d-flex justify-content-center"><%= render "shared/message/messages" %></span>
          <%= yield %>
        </main>

        <!-- 右サイドバー -->
        <div class="sidebar sidebar-right">
          <h5>おすすめ</h5>
          <ul class="list-unstyled">
            <li><%= link_to '人気投稿', root_path, class: 'nav-link' %></li>
            <li><%= link_to '新着ユーザー', root_path, class: 'nav-link' %></li>
          </ul>
        </div>
      
      </div>
    </div>

    <!-- フッター -->
    <%= render 'shared/footer/footer' %>

  </body>
</html>